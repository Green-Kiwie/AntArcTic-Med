

export default function useInitializeContext(setGameRunning, setMetrics, user){
    //number of rounds left
    //gamestate, either "displaying correct image" or "selecting image"
    //imageid or imageurl. something to keep track of correct image

    return //returns a useMemo object that has all the required context data set up. refer to switch it up game
}